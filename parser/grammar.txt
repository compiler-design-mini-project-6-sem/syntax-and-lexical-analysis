P → BEGIN STMTS END
STMTS → STMTS STMT | STMT
STMT → DECL | ASSIGN | PRINT | FOR

DECL → TYPE ID_LIST
TYPE → INTEGER | REAL | STRING
ID_LIST → ID | ID , ID_LIST

ASSIGN → ID := VALUE
VALUE → NUM | STRING

PRINT → PRINT STRING

FOR → FOR ID := NUM TO NUM STMTS